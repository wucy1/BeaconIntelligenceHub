# Cloudflare Workers static assets (Vite build output).
# Used when connecting repo via Workers Builds (deploy command: npx wrangler deploy).

name = "beacon"
compatibility_date = "2024-12-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
